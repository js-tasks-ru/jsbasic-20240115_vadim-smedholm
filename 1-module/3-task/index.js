function ucFirst(str) {
    if (str != '' && str.length>1 );{
      let firstsymbol = str.charAt(0);
      let upperfirastsymbol = firstsymbol.toUpperCase();
      let resultstr = str.slice(1);
    return str = upperfirastsymbol+resultstr;}

  return str;
  }// ваш код...
